#!/usr/bin/env python


def options(opt):
    pass


def configure(conf):
    pass


def build(bld):
    bld(features='fc fcshlib',
        source='configstrings.f90',
        target='configstrings')
