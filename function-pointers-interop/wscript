#!/usr/bin/env python


def options(opt):
    pass


def configure(conf):
    pass


def build(bld):
    bld(features='fc fcshlib',
        source='funcpointerinterop.f90',
        target='funcpointerinterop')
